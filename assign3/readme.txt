In order to compile the program, please type:

gcc -o smallsh smallsh.c

That should give you an executable that is compatible with the test script. You may also run the code
manually using:

./smallsh